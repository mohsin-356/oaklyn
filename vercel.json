{
  "version": 2,
  "builds": [
    {
      "src": "api/[[...path]].js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/((?!api/).*)", "dest": "/index.html" }
  ]
}
